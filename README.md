# 포켓몬 얼굴 프로필 생성기

얼굴 사진을 올리면 브라우저에서 얼굴 특징을 분석해 타입, 특성, 종족값, 기술, 도감 설명을 생성하는 실험용 웹앱이다.

## 실행 방법

이 프로젝트는 ES 모듈과 로컬 JSON 파일을 사용하므로 `index.html`을 바로 여는 대신 정적 서버로 실행하는 편이 좋다.

예시:

```bash
python -m http.server 8000
```

그 뒤 브라우저에서 `http://localhost:8000`으로 접속하면 된다.
